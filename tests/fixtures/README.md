# Fixtures (fasit)

Generert fra `sources/` av `scripts/oracle_*.py` — aldri fra `athletics_scoring`. Hver fil låses etter Simens
review (se `docs/BACKLOG.md`). **Agenten endrer aldri filer her.** Mistenkes feil fasit: sett oppgaven til
`Blokkert` i backloggen og stopp.
